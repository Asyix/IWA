import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("Page de Profil 👤")
                .font(.title)
                .padding()
        }
        .navigationTitle("Profil")
    }
}
