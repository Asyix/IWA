import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Paramètres ⚙️")
                .font(.title)
                .padding()
        }
        .navigationTitle("Paramètres")
    }
}
