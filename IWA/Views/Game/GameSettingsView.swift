import SwiftUI

struct GameSettingsView: View {
    var body: some View {
        VStack {
            Text("Réglages des jeux ⚙️")
                .font(.title)
                .padding()
        }
        .navigationTitle("Réglages Jeux")
    }
}
